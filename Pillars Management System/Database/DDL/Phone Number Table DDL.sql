-- Start Code

CREATE TABLE Pillars_Phone_Number(
	ID INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	Customer_ID INT NOT NULL REFERENCES Pillars_Customer(ID) ON DELETE CASCADE,
	Tel CHAR(13) UNIQUE CHECK(Tel LIKE('+962%') AND LENGTH(Tel) = 13) NOT NULL
);

-- End Code